# Copy this file to `.env` and fill in your real credentials (DO NOT commit real credentials)
# Example values showing the user's DB name and password (use carefully):
DB_HOST=localhost
DB_PORT=3306
DB_NAME=class
DB_USER=root
DB_PASSWORD=kelly123

# Other environment variables used by the backend (examples)
PORT=5000
JWT_SECRET=your_jwt_secret_here
