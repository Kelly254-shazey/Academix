# Fix React Hooks Rules Violations in StudentPortal.jsx

## Tasks
- [ ] Move the `if (!user)` check after all hook declarations
- [ ] Add guard clause `if (!user) return;` in the first useEffect
- [ ] Verify ESLint errors are resolved
- [ ] Test component functionality
